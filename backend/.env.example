PORT=3001

WORLDCUP_API_URL=http://worldcup26.ir:3050
WORLDCUP_API_TIMEOUT_MS=25000
WORLDCUP_CACHE_TTL_MS=300000

# --- Railway MySQL (copiá desde Railway → MySQL service → Variables) ---
# Opción A: URL pública (recomendada para Vercel + setup local)
# MYSQL_PUBLIC_URL=mysql://root:password@host.railway.app:port/railway

# Opción B: variables individuales
# MYSQLHOST=xxx.railway.app
# MYSQLPORT=3306
# MYSQLUSER=root
# MYSQLPASSWORD=tu_password
# MYSQLDATABASE=railway

# --- MySQL local (desarrollo sin Railway) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=grandt

CORS_ORIGIN=http://localhost:3000
CURRENT_MATCHDAY=2
