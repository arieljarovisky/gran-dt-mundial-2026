PORT=3001

# JWT para sesiones (cambiá en producción)
JWT_SECRET=genera-un-secreto-largo-y-aleatorio

WORLDCUP_API_URL=http://worldcup26.ir:3050
WORLDCUP_API_TIMEOUT_MS=25000
WORLDCUP_CACHE_TTL_MS=300000
CURRENT_MATCHDAY=2

# URL del frontend (Vercel) — CORS en producción
# FRONTEND_URL=https://tu-app.vercel.app

# --- Railway MySQL (Variables del servicio MySQL en Railway) ---
# En el servicio backend, referenciá las vars del MySQL o pegá la URL:
# MYSQL_URL=mysql://root:password@mysql.railway.internal:3306/railway
# MYSQL_PUBLIC_URL=mysql://root:password@host.railway.app:port/railway

# --- MySQL local (desarrollo) ---
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=grandt

CORS_ORIGIN=http://localhost:3000

# --- API-Football (estadísticas de última temporada para precios IVM) ---
# Clave gratuita en https://www.api-football.com/ (100 req/día gratis)
# Pasos: 1) API_FOOTBALL_KEY=xxx node scripts/fetch-player-stats.js
#         2) node scripts/recalc-prices-ivm.js
API_FOOTBALL_KEY=tu_clave_aqui
